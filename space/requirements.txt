# WaveDiT ZeroGPU Space - lean generation-only dependencies.
#
# IMPORTANT (ZeroGPU contract):
#  * torch MUST be listed and pinned to a ZeroGPU-SUPPORTED version. Per the ZeroGPU
#    docs, supported PyTorch is 2.8.0..latest (2.8.0 / 2.9.1 / 2.10.0 / 2.11.0); the
#    repo default 2.6.0 is NOT supported (pinning it fails the hardware match with
#    "No candidate PyTorch version found for ZeroGPU"). The checkpoints are plain
#    state_dicts and load fine on 2.8. Do NOT install the `wavedit` package itself
#    (its pyproject pins torch==2.6.0) - the wavedit/ source is VENDORED here instead.
#  * Do NOT pin `spaces` - the platform pins its own; a conflicting pin breaks
#    pip resolution at build time. List it unpinned.
#  * wandb / matplotlib / monai / pandas / scikit-image / torchvision / flow-matching
#    are NOT needed on the sampling path and are intentionally omitted (wandb is made
#    optional via the vendored lazy-import patch in wavelet_flow_matching.py and
#    trainer.py, and the only other wandb importer -- evaluation/visualization.py --
#    is pruned from the vendored tree entirely).

torch==2.8.0
gradio==6.17.3
spaces
numpy
einops==0.8.0
nibabel==5.3.2
pyyaml>=6.0
pywavelets==1.8.0
tqdm==4.67.1
huggingface_hub
